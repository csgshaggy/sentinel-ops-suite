$blue = "`e[34m"
$reset = "`e[0m"
$bold = "`e[1m"

Write-Host "$blue==============================================================$reset"
Write-Host "$bold   SSRF COMMAND CONSOLE — OPERATOR UTILITY SUITE$reset"
Write-Host "$blue==============================================================$reset"
