name: Combined CI

on:
  workflow_run:
    workflows:
      - Avatar Integrity
      - Import Validation
      - Build and Test
      - Lint
      - Type Check
    types:
      - completed

jobs:
  aggregate:
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate CI status
        run: echo "All dependent workflows completed."

