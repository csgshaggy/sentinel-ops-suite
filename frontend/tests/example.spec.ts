import { expect,test } from "vitest";

test("dashboard loads", () => {
  // Placeholder: Vitest cannot load a real browser page.
  // Replace with component tests later if needed.
  expect(true).toBe(true);
});
