// frontend/src/routes.jsx
// ------------------------------------------------------------
// Legacy placeholder — this file is intentionally NOT used.
// It exists only to prevent import errors from older code.
// ------------------------------------------------------------

import React from "react";

// If any old code imports this file, it will render nothing
// and will NOT create a second Router (which breaks the app).

export default function LegacyRoutes() {
  return null;
}
