import React from "react";

const RouterDrift = () => {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Router Drift</h1>
      <p>
        Detection of routing inconsistencies, mismatched route definitions, and
        drift between frontend and backend route maps.
      </p>
    </div>
  );
};

export default RouterDrift;
